import { toAscii } from '../../../helpers/to-ascii';
import { module, test } from 'qunit';

module('Unit | Helper | to ascii');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = toAscii(42);
  assert.ok(result);
});
